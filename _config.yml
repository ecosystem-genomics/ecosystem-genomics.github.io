# Site settings
title: BRIDGES 
description: > # this means to ignore newlines until "baseurl:"
#baseurl: "/blog/" # the subpath of your site, e.g. /blog/
url: "https://ecosystem-genomics.github.io/" # the base hostname & protocol for your site
author: Copyright &copy; 2021 - BRIDGES 
lang: en_US

# Build settings
safe: true # always trues on github-pages, disables _plugins/
markdown: kramdown
sass:
  indentWidth: 4
  style: compressed
  precision: 10
plugins:
  - jekyll-sitemap
  - jekyll-seo-tag
exclude:
  - Gemfile
  - Gemfile.lock
  - build-in-docker.sh
  - README.md
#Google Analytisc
google_analytics: G-NPQZRDNS4Z

#run on M1: bundle exec jekyll serve